[project]
name = "myllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "huggingface-hub>=1.12.0",
    "numpy>=2.4.4",
    "pyarrow>=24.0.0",
    "tiktoken>=0.12.0",
    "torch>=2.11.0",
]
